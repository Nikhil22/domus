'use strict';

module.exports = function (app) {
    const userCtrl = require('../controllers/user');
    // TODO: debug
    //app.route('/user/onboardingSubmitted').post(userCtrl.onboardingSubmitted);
};
